language: python
python:
    - "2.7"
env:
  - DJANGO_VERSION=1.7.9
install: "pip install -r requirements.txt"
script: 
    - "coverage run --source='.' manage.py test repository"
before_install:
  - pip install codecov
  - python manage.py makemigrations
  - python manage.py migrate
after_success:
  - codecov
